# SonnetDB Parity Results

This orphan branch is maintained by `.github/workflows/parity.yml`.
`latest.md` and `latest.json` contain the newest full-profile summary.
If the full artifact is missing, they explicitly report `NOT_READY`;
such a run is retained for diagnostics and is not release evidence.
