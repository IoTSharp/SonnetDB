# GitHub Issues Roadmap Snapshot

This page records the public issue backlog observed on 2026-09-21. The machine-readable source is [`github-issues-20260921.json`](audits/github-issues-20260921.json). The historical snapshot contains 25 open issues, no closed issues, and no issue labels or milestones; per-issue update times and URLs are preserved in the snapshot metadata. The verified current state after commit `9c4de6e9` is 17 `closed_implemented_scope` issues and 8 remaining open issues. A closed scope means the documented bounded contract was implemented, tested, commented on, and closed on GitHub; residual parity, hardware, production, or broader-dialect evidence remains explicitly tracked.

`GH-Issue #N` always means the external GitHub issue. Internal roadmap work items keep their existing `#N` notation. The numbers overlap (for example, GH-Issue #174 is a string-function request while internal M25 #174 is Document capacity), so the prefix is required in roadmap and changelog text.

## Execution Order

1. **SQL foundation:** GH-Issue #171 -> #178 -> #172 -> #173 -> #174 -> #175 -> #176 -> #179 -> #181 -> #182 -> #183 -> #192. GH-Issue #189 (`WITH RECURSIVE`) follows #171 and #178.
2. **SQL DML, concurrency and DDL:** GH-Issue #177 -> #191/#184 -> #190; then #187 -> #188 after catalog and metadata contracts are fixed.
3. **Model, driver and management boundaries:** GH-Issue #193 -> #186 -> #185 -> #180.
4. **Existing-surface review:** GH-Issue #89 and #91 were mapped to existing M27/M29 entry points, reviewed against the delivered contracts, and closed with their remaining deployment evidence retained in the roadmap; no duplicate implementation is planned.

The queue belongs to M43 Step 2, after the current M27 #340 implementation residuals and before the performance and post-deployment evidence queues. SQL planner and execution contracts should be reused rather than creating a second parser or planner.

## Current Status

The historical snapshot had 25 open entries. The current state has 17 `closed_implemented_scope` entries: GH-Issue #89, #91, #171, #172, #173, #174, #175, #176, #178, #179, #181, #182, #183, #185, #186, #188 and #192. They are closed only for the bounded contracts documented below; pending remote parity, Frame, fixed-hardware, deployment and production evidence is not represented as complete. The remaining eight entries are still `open`: GH-Issue #177, #180, #184, #187, #189, #190, #191 and #193. Each implementation has a deterministic regression test, capability-index entry and explicit `PASS`, `NOT_READY`, or `DEFERRED` boundary.

GH-Issue #171 evidence boundary: `PASS` for local Core parsing and execution of single/multiple non-recursive CTEs in `FROM`, `IN`, and correlated `EXISTS`, including ordered CTE-to-CTE references. CTE output-column lists and `WITH RECURSIVE` remain explicit unsupported boundaries; the dedicated `SqlCteTests` regression passed 5/5. Remote parity and external issue-thread confirmation remain pending.

GH-Issue #178 evidence boundary: `PASS` for local Core parsing and execution of `UNION ALL`, `INTERSECT`, and `EXCEPT`, including deterministic `ORDER BY` results and column-count diagnostics; the dedicated `SqlSetOperationTests` regression covers duplicate preservation and set difference/intersection. Compound operators are currently evaluated left-to-right; remote parity and external issue-thread confirmation remain pending.

GH-Issue #172 evidence boundary: `PASS` for local Core parsing and execution of `OVER ()`, `OVER (ORDER BY time ASC)`, `row_number()`, and existing measurement window functions with explicit time ordering; the dedicated `SqlAnsiWindowTests` regression passes 5/5. `PARTITION BY`, non-time/descending order, `ROWS`/`RANGE` frames, relation/JOIN paths, remote parity and external issue-thread confirmation remain pending.

GH-Issue #173 evidence boundary: `PASS` for local Core parsing and literal/column execution of `CAST(expr AS type)` across measurement, relation, document, join, JSON-file, hybrid, and vector-search evaluators. `INT`, `FLOAT`, `BOOL`, `STRING`, `DATETIME`, `BLOB`, and `JSON` targets share NULL propagation and deterministic conversion errors; `VECTOR` and `GEOPOINT` targets are explicit unsupported boundaries. The dedicated `SqlCastTests` regression passes 9/9. Remote parity and external issue-thread confirmation remain pending.

GH-Issue #174 evidence boundary: `PASS` for local Core registration and SQL relation projection/filter execution of `trim`/`ltrim`/`rtrim`, `length`/`char_length`, `substring`/`substr`, `replace`, `left`/`right`, and ordinal `starts_with`/`ends_with`/`contains`; NULL propagation, 1-based positions, non-negative counts and deterministic string/integer diagnostics are covered by `FunctionRegistryTests` and `SqlStringFunctionTests`. The subset is intentionally bounded to local Core semantics; SQL dialect forms such as `TRIM(... FROM ...)`, collation-sensitive matching, byte-length functions and remote parity remain pending, as does external issue-thread confirmation.

GH-Issue #175 evidence boundary: `PASS` for local Core registration and SQL relation projection/filter execution of `date_diff`/`datediff`, `date_format`/`format_datetime`/`to_char`, and `strftime`; temporal inputs are DATETIME, DateTimeOffset or Unix milliseconds, date differences are bounded to documented parts, formats are capped at 128 characters, and NULL/invalid-part/invalid-marker behavior is deterministic. `SqlDateFunctionTests` and `FunctionRegistryTests` cover the local subset; locale-sensitive formatting, timezone database/calendar semantics, remote parity and external issue-thread confirmation remain pending.

GH-Issue #176 evidence boundary: `PASS` for existing local Core parser/AST and measurement/relationship aggregation paths with `COUNT/SUM/AVG/MIN/MAX(DISTINCT field)`, duplicate elimination, NULL exclusion and rejection of `COUNT(DISTINCT *)`; the dedicated `SqlAggregateDistinctTests` regression passes 4/4. DISTINCT input is currently bounded to one aggregate field and local Core semantics; multi-expression DISTINCT, ordered-set/window DISTINCT, remote parity and external issue-thread confirmation remain pending.

GH-Issue #179 evidence boundary: `PASS` for local Core lexer/parser and relation/measurement execution of inclusive `BETWEEN` / `NOT BETWEEN` and invariant `ILIKE` / `NOT ILIKE`, including wildcard matching and NULL exclusion in filters; `SqlBetweenIlikeTests` passes 3/3. The implementation desugars range predicates to existing comparison/three-valued logic and ILIKE to invariant lower+LIKE; symmetric ranges, locale-specific collations, remote parity and external issue-thread confirmation remain pending.

GH-Issue #181 evidence boundary: `PASS` for local Core `DECIMAL`/`NUMERIC` parsing, exact `System.Decimal` CAST and arithmetic, relation-table persistence with bounded `DECIMAL(precision, scale)` metadata, numeric primary-key range filtering, equality-key lookup, and embedded/remote ADO.NET `Columns` precision/scale projection when the schema contract carries it. Decimal values use a 16-byte payload and schema format v9; decimal equality keys are canonical text, while decimal range planning deliberately falls back to scans because the text key is not a numeric sort order. Non-finite floating inputs, precision/scale outside 1..38, overflow/scale enforcement beyond `System.Decimal`, remote Frame parity and external issue-thread confirmation remain explicit pending boundaries. `SqlDecimalTests` passes 5/5, `SqlCastTests` passes 9/9, and the ADO metadata regression covers `DECIMAL(18,4)`.

GH-Issue #182 evidence boundary: `PASS` for local Core `TIME` parsing/CAST, relation-table `TimeOnly` tick persistence with schema format v10, equality/order comparisons, NULL handling, strict `[00:00:00,24:00:00)` validation, `TimeOnly`/`TimeSpan` parameter binding and embedded ADO `GetSchema`/`GetFieldType`; `SqlTimeTests` passes 3/3 and the ADO TIME regression passes 1/1. `DATETIME`/`DateTimeOffset` to `TIME` conversion is explicitly rejected. Cross-day and `24:00:00` semantics, typed remote TIME restoration/Frame parity and external issue-thread confirmation remain pending.

GH-Issue #183 evidence boundary: `PASS` for the local Core lexer/parser and relation SELECT/WHERE/UPDATE contract; regression command `dotnet test tests/SonnetDB.Core.Tests/SonnetDB.Core.Tests.csproj --no-restore --filter "FullyQualifiedName~SqlLexerTests|FullyQualifiedName~SqlParserTests|FullyQualifiedName~SqlExpressionExecutionTests"` passed 158/158. Remote ADO.NET/Frame parity and external issue-thread confirmation remain pending.

GH-Issue #186 evidence boundary: `PASS` for embedded VECTOR parameter binding, finite `float[]`/memory input validation, explicit NDJSON float-array writing, REST remote execution, and numeric-array decoding with `GetFieldType() == typeof(float[])`; `SqlVectorParameterTests` passed 3/3 and `RemoteVectorParameterTests` passed 2/2 against a real Kestrel server. Remote Frame E2E and external issue-thread confirmation remain pending.

The external issue list is intentionally separate from the existing nine-model gap catalog and from the internal PR sequence #382~#402. Closed scopes retain the issue URL, commit, test command and remaining boundary here and in the capability evidence index; the eight open issues remain in the execution queue.
