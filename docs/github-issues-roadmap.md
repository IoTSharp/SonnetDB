# GitHub Issues Roadmap Snapshot

The [2026-09-25 GitHub read-back](audits/github-pr-queue-20260925.md) initially found nine open issues: #184, #187, #189, #190, #191, #194, #195, #197 and #198. The [current 30-issue read-back](audits/github-issues-complete-audit-20260925.md) supersedes that initial state: 26 closed and four open (#91, #184, #186 and #197). The [combined validation](audits/online-pr-issue-integration-20260925.md) records earlier implementation and seven dependency PRs, which Dependabot closed as superseded after their exact version changes reached `main`. The [`2026-09-23 JSON snapshot`](audits/github-issues-20260923.json) and [`2026-09-21 snapshot`](audits/github-issues-20260921.json) remain historical evidence. Local implementation does not by itself change issue state; closed issues retain the scope and remaining boundaries recorded at their closure, without implying whole-milestone, hardware or production completion.

`GH-Issue #N` always means the external GitHub issue. Internal roadmap work items keep their existing `#N` notation. The numbers overlap (for example, GH-Issue #174 is a string-function request while internal M25 #174 is Document capacity), so the prefix is required in roadmap and changelog text.

## Execution Order

1. **SQL foundation:** GH-Issue #171 -> #178 -> #172 -> #173 -> #174 -> #175 -> #176 -> #179 -> #181 -> #182 -> #183 -> #192. GH-Issue #189 (`WITH RECURSIVE`) follows #171 and #178.
2. **SQL DML, concurrency and DDL:** GH-Issue #177 -> #191/#184 -> #190; then #187 -> #188 after catalog and metadata contracts are fixed.
3. **Model, driver and management boundaries:** GH-Issue #193 -> #186 -> #185 -> #180.
4. **Existing-surface review:** GH-Issue #89 is closed for its MCP design-document scope. GH-Issue #91 was reopened: Studio now mounts an existing embedded database, but clean offline Windows installation and WebView2 first-run evidence remain open.

New requests retain those dependency groups: GH-Issue #196 follows #177's normalized JOIN repair; #194 and #197 cover DML result contracts alongside #184/#191; #195 follows their insertion, transaction and RETURNING semantics; #198 belongs to model/driver boundaries. Independent acceptance work may run in parallel, but issue state changes require completed implementation and verification.

The queue belongs to M43 Step 2, after the current M27 #340 implementation residuals and before the performance and post-deployment evidence queues. SQL planner and execution contracts should be reused rather than creating a second parser or planner.

## Current Status

The authenticated GitHub read-back on 2026-09-25 found 30 Issues: 26 closed with `state_reason=completed` and four open. The closed set is GH-Issue #89, #171–#183, #185, #187–#196 and #198; the open set is #91, #184, #186 and #197. The [per-issue audit](audits/github-issues-complete-audit-20260925.md) records the evidence and remaining gates. Earlier bounded closure notes below describe their evidence at that time; references to pending external confirmation in those historical notes are superseded by the current GitHub state.

On 2026-09-23, GH-Issue #177, #180 and #193 completed embedded, real REST, HTTP/2 Frame and model-boundary acceptance, with 358/358 passing regressions. Code was pushed through `6ca5ce7a939a7b62db2e82a37f1ae4d2827b43c3`, then all three issues were closed as `completed` and read back from GitHub. See [`sql-provider-closure-20260923.md`](audits/sql-provider-closure-20260923.md). Fixed-hardware, production, long-run and external database comparison evidence remain separate roadmap work.

| Open request | Required closure scope |
|---|---|
| GH-Issue #91 | Clean offline Windows MSI/WebView2 installation and first-run evidence for the existing embedded-database Studio workflow. |
| GH-Issue #184 | Remote session is implemented on `main`; release version and deployed failure/recovery evidence remain. |
| GH-Issue #186 | Parameterized measurement VECTOR UPDATE with one consistent raw/SQL/KNN replacement and recovery contract. |
| GH-Issue #197 | Main-source result behavior is tested; the first complete released version, installed-package and compatibility evidence remain. Published 3.1.0 is not sufficient. |

GH-Issue #171 evidence boundary: the 2026-09-25 follow-up supports ordered output-column lists for non-recursive CTEs, including JOIN, aggregation, parameterized `IN`/`EXISTS`, ordering/pagination, set operations and relation/measurement/document sources. Empty-result width mismatch and duplicate names fail explicitly; embedded ADO preserves the renamed name and Int64 type. The dedicated `SqlCteTests` regression passed 13/13. Bounded `WITH RECURSIVE` is tracked separately under #189. Remote parity and the external issue-thread follow-up remain pending. See [the #171 audit](audits/github-issue-171-cte-columns-20260925.md).

GH-Issue #178 evidence boundary: local Core parsing and execution covers `UNION ALL`, `INTERSECT`, and `EXCEPT`, including standard `INTERSECT` precedence, final sorting/pagination, input order for unordered `UNION ALL`, `NULL`/duplicate/multicolumn equality, column-count and type diagnostics, and bounded set-state admission. The set operator rejects when its estimated retained rows or hash state exceed the query/database memory budget; it does not spill and does not claim a hard CLR heap peak bound. Remote parity and external issue-thread confirmation remain separate.

GH-Issue #172 evidence boundary: measurement 的 `OVER ()`、`OVER (ORDER BY time ASC)`、`row_number()` 与已有时序窗口保持原合同。后续独立实现为关系表 `ROW_NUMBER`、`COUNT/SUM/AVG/MIN/MAX` 增加多列表达式 `PARTITION BY`、多列升降序窗口排序、无排序全分区聚合及 peer-aware 默认累计帧；关系窗口及嵌入式 ADO 回归 11/11 已通过，完整 Core 回归结果见 [专项审计](audits/issue-172-window-20260925.md)。显式 `ROWS`/`RANGE` frame、嵌套窗口表达式、同层普通聚合混用仍拒绝；有序累计聚合最坏 O(n²)。远程协议尚未取证。这是本地分支证据，须合并并回读远端后才能更新线上 issue 线程。

GH-Issue #173 evidence boundary: `PASS` for local Core parsing and literal/column execution of `CAST(expr AS type)` across measurement, relation, document, join, JSON-file, hybrid, and vector-search evaluators. `INT`, `FLOAT`, `BOOL`, `STRING`, `DATETIME`, `BLOB`, and `JSON` targets share NULL propagation and deterministic conversion errors; `VECTOR` and `GEOPOINT` targets are explicit unsupported boundaries. The dedicated `SqlCastTests` regression passes 9/9. Remote parity and external issue-thread confirmation remain pending.

GH-Issue #174 evidence boundary: `PASS` for local Core registration and SQL relation projection/filter execution of `trim`/`ltrim`/`rtrim`, `length`/`char_length`, `substring`/`substr`, `replace`, `left`/`right`, and ordinal `starts_with`/`ends_with`/`contains`; NULL propagation, 1-based positions, non-negative counts and deterministic string/integer diagnostics are covered by `FunctionRegistryTests` and `SqlStringFunctionTests`. The subset is intentionally bounded to local Core semantics; SQL dialect forms such as `TRIM(... FROM ...)`, collation-sensitive matching, byte-length functions and remote parity remain pending, as does external issue-thread confirmation.

GH-Issue #175 evidence boundary: `PASS` for local Core registration and SQL relation projection/filter execution of `date_diff`/`datediff`, `date_format`/`format_datetime`/`to_char`, and `strftime`; temporal inputs are DATETIME, DateTimeOffset or Unix milliseconds, date differences are bounded to documented parts, formats are capped at 128 characters, and NULL/invalid-part/invalid-marker behavior is deterministic. `SqlDateFunctionTests` and `FunctionRegistryTests` cover the local subset; locale-sensitive formatting, timezone database/calendar semantics, remote parity and external issue-thread confirmation remain pending.

GH-Issue #176 evidence boundary: `PASS` for existing local Core parser/AST and measurement/relationship aggregation paths with `COUNT/SUM/AVG/MIN/MAX(DISTINCT field)`, duplicate elimination, NULL exclusion and rejection of `COUNT(DISTINCT *)`; the dedicated `SqlAggregateDistinctTests` regression passes 4/4. DISTINCT input is currently bounded to one aggregate field and local Core semantics; multi-expression DISTINCT, ordered-set/window DISTINCT, remote parity and external issue-thread confirmation remain pending.

GH-Issue #179 evidence boundary: `PASS` for local Core lexer/parser and relation/measurement execution of inclusive `BETWEEN` / `NOT BETWEEN` and invariant `ILIKE` / `NOT ILIKE`, including wildcard matching and NULL exclusion in filters; `SqlBetweenIlikeTests` passes 3/3. The implementation desugars range predicates to existing comparison/three-valued logic and ILIKE to invariant lower+LIKE; symmetric ranges, locale-specific collations, remote parity and external issue-thread confirmation remain pending.

GH-Issue #181 evidence boundary: `PASS` for local Core `DECIMAL`/`NUMERIC` parsing, exact `System.Decimal` CAST and arithmetic, relation-table persistence with bounded `DECIMAL(precision, scale)` metadata, numeric primary-key range filtering, equality-key lookup, and embedded/remote ADO.NET `Columns` precision/scale projection when the schema contract carries it. Decimal values use a 16-byte payload and schema format v9; decimal equality keys are canonical text, while decimal range planning deliberately falls back to scans because the text key is not a numeric sort order. Non-finite floating inputs, precision/scale outside 1..38, overflow/scale enforcement beyond `System.Decimal`, remote Frame parity and external issue-thread confirmation remain explicit pending boundaries. `SqlDecimalTests` passes 5/5, `SqlCastTests` passes 9/9, and the ADO metadata regression covers `DECIMAL(18,4)`.

GH-Issue #182 evidence boundary: `PASS` for local Core `TIME` parsing/CAST, relation-table `TimeOnly` tick persistence with schema format v10, equality/order comparisons, NULL handling, strict `[00:00:00,24:00:00)` validation, `TimeOnly`/`TimeSpan` parameter binding and embedded ADO `GetSchema`/`GetFieldType`; `SqlTimeTests` passes 3/3 and the ADO TIME regression passes 1/1. `DATETIME`/`DateTimeOffset` to `TIME` conversion is explicitly rejected. Cross-day and `24:00:00` semantics, typed remote TIME restoration/Frame parity and external issue-thread confirmation remain pending.

GH-Issue #183 evidence boundary: `PASS` for the local Core lexer/parser and relation SELECT/WHERE/UPDATE contract; regression command `dotnet test tests/SonnetDB.Core.Tests/SonnetDB.Core.Tests.csproj --no-restore --filter "FullyQualifiedName~SqlLexerTests|FullyQualifiedName~SqlParserTests|FullyQualifiedName~SqlExpressionExecutionTests"` passed 158/158. Remote ADO.NET/Frame parity and external issue-thread confirmation remain pending.

GH-Issue #186 evidence boundary: embedded, REST and HTTP/2 ADO support finite `float[]` INSERT and `float[]` projection/centroid/KNN results; direct read-only SQL Frame requests also accept native float32 VECTOR parameters. Real Kestrel tests verify Frame reads, HTTP/2 REST write fallback, metadata, sparse NULL and async reads. Measurement UPDATE now rejects with an explicit no-replacement diagnostic; [the #186 audit](audits/github-issue-186-frame-vector-20260925.md) reproduces why same-timestamp INSERT cannot substitute for it and decomposes the required WAL/segment/KNN recovery work. Parameterized VECTOR UPDATE remains unsupported and the issue remains open.

GH-Issue #177 is **complete and closed** as of 2026-09-23: relation RIGHT/FULL/CROSS semantics, column/type/order rules, LEFT compatibility and ADO capability discovery pass embedded, real REST and HTTP/2 Frame verification. Normalized measurement JOIN/EXPLAIN now consistently preserves the supported INNER path and rejects outer/CROSS joins. Dedicated Core 9/9 and real-server 23/23 pass; see the [closure report](audits/sql-provider-closure-20260923.md). Production performance is a separate gate.

GH-Issue #180 is **complete and closed** as of 2026-09-23: JSON exists/length/containment, bound paths, NULL/missing distinctions, resource errors and documented index collaboration pass Core 14/14 and embedded/real REST/HTTP2 Frame 24/24. Overlapping array candidates use bounded reassignment rather than order-dependent greedy selection. EXPLAIN verifies indexed `json_value` equality plus JSON residuals, and a scan for standalone containment. Dedicated containment index pushdown and large-corpus performance remain additional optimization work. See the [JSON contract](json-query-contract.md).

GH-Issue #184 evidence boundary: the local Core relation-table contract covers `ON CONFLICT (...) DO UPDATE SET`, `excluded.column` expressions, defaults, explicit transactions, `RETURNING`, generated `ROWVERSION`, duplicate-assignment rejection and candidate `NOT NULL` validation. The new server transaction session on `main` performs remote ADO `DO UPDATE ... RETURNING` in the actual pending transaction and commits without SQL replay; old Servers remain fail closed. Integrated Release Core 75/75 and complete real-Kestrel Server 1106/1106 passed. Server and CLI win-x64 NativeAOT publishes on the isolated branch had no IL/AOT warnings. The [remote session audit](audits/github-issue-184-remote-session-20260925.md) records credential binding, expiry, idempotency and remaining deployment/release-version boundaries; the earlier [call-chain audit](audits/github-issue-184-conditional-upsert-20260925.md) describes why the former request-scoped protocol was insufficient. External issue-thread confirmation is still pending.

GH-Issue #187 is **complete and closed under the issue's constrained in-place route**. The original keyless empty-table example now persists and evolves through `AUTO_INCREMENT`, primary key and `ROWVERSION`; writes before a primary key fail with a stable code before identity allocation. Existing rows' primary-key changes remain explicitly unsupported. Release Core 5197/5197 and real-service DDL 4/4 passed before integration; integrated Core 53/53 and real-service 5/5 passed. The code reached `main` in `eb54af54`; see the [DDL audit](audits/github-issue-187-ddl-evolution-20260925.md) and [closure comment](https://github.com/IoTSharp/SonnetDB/issues/187#issuecomment-5824905455).

GH-Issue #189 evidence boundary: `PARTIAL` for a single `WITH RECURSIVE` CTE with frontier evaluation, cycle deduplication, candidate/retained-row limits, cancellation, `EXPLAIN`, and real REST/Frame reads. `2cfa98fa` added conservative row admission around relational JOIN build sides, ordinary CTE materialization, sort and aggregate inputs; Core 53/53 and real-service 5/5 integrated checks passed. Table decoding, expression temporaries, temporary JOIN outputs and non-relation SELECT dispatch can still allocate before admission, so the strict peak-memory gate remains open in the [recursive CTE audit](audits/github-issue-189-recursive-cte-20260925.md).

GH-Issue #190 is **complete and closed** under its documented optimistic-concurrency route. Core 8/8 and real Kestrel 5/5 passed before the [closure comment](https://github.com/IoTSharp/SonnetDB/issues/190#issuecomment-5824739550) and completed state read-back. `SELECT FOR UPDATE`/`NOWAIT`/`SKIP LOCKED` reject with `sql_locking_read_unsupported`; `ROWVERSION` is the supported workflow. Deployed-client evidence remains separate in the [locking-read audit](audits/github-issue-190-locking-read-boundary-20260925.md).

GH-Issue #191 is **complete and closed** for relation-table `UPDATE ... JOIN`/`FROM` INNER/LEFT, duplicate-source first match, target-key de-duplication, parameters, NULL/composite keys, triggers, `RETURNING`, `ROWVERSION`, constraints and rollback. Unique source keys now use an indexed nested loop while non-unique sources keep declaration order. Release Core 121/121 and real Kestrel/HTTP2 101/101 passed; code reached `main` in `d6212ad7` before the [closure comment](https://github.com/IoTSharp/SonnetDB/issues/191#issuecomment-5824857929). Fixed-hardware and deployed-server evidence remain separate in [the #191 acceptance record](audits/issue-191-update-join-20260925.md).

GH-Issue #194 is **complete and closed** for UPDATE final-row and DELETE pre-image `RETURNING`, target-only affected counts, full-pre-image concurrency validation, remote lightweight-transaction preview ordering, and declared result metadata even when no rows match. Release Core 121/121 and real Kestrel/HTTP2 101/101 passed; code reached `main` in `2ba84b8d` before the [closure comment](https://github.com/IoTSharp/SonnetDB/issues/194#issuecomment-5824861087). Frame SQL remains read-only and HTTP/2 ADO writes use REST; the [DML acceptance record](audits/issue-194-update-delete-returning-20260925.md) includes a prior NativeAOT CLI publish.

GH-Issue #195 evidence boundary: `PARTIAL` for relation-table `INSERT ... SELECT` projection, generated values, same-table source snapshot, conflicts, transactions and embedded/real remote ADO plus REST/HTTP2 acceptance. Its post-materialization transition-row budget does not cap source SELECT peak memory; published-package and fixed-hardware gates remain open in the [INSERT SELECT audit](audits/insert-select-195-20260925.md).

GH-Issue #197 evidence boundary: `PASS` for relation-table `INSERT ... RETURNING` order, generated values, empty results, affected counts, stable composite-key errors, and embedded/remote ADO metadata with REST/HTTP2 fallback. Native Frame DML remains read-only, and the full contract is not in published 3.1.0; see the [INSERT RETURNING audit](audits/github-issue-197-insert-returning-20260925.md).

GH-Issue #198 evidence boundary: `PARTIAL` for a documented signed-Int64/STRING boundary, deterministic overflow and CLR `BigInteger` rejection, exact DECIMAL transport, and declared relation SELECT types through embedded ADO, REST NDJSON and HTTP/2 Frame on empty/all-NULL results. Dynamic and non-relation projections remain conservatively typed; old Frame clients need protocol negotiation or a stated minimum version before release. See the [Int64 and metadata audit](audits/github-issue-198-int64-boundary-20260925.md).

GH-Issue #193 is **complete and closed under the issue's product-boundary option 2** as of 2026-09-23. The type matrix, SQL reference and SDK-local `GetSchema("DataTypes")` identify VECTOR/GEOPOINT as measurement FIELD types, and the [official relation alternatives](relation-type-boundary.md) are documented and exercised. Fifteen integration cases verify CREATE/ALTER rejection without partial schemas, local capability metadata, real relation directory queries and typed FIELD/alternative-model SELECTs across embedded, REST and HTTP/2 Frame. This does not add native relation VECTOR/GEOPOINT columns.

GH-Issue #196 is **complete and closed** as of 2026-09-23. Normalized JOIN and ORDER BY fields preserve bound measurement-to-relation INNER queries, hidden FIELD sorting, mixed-direction multiple keys and pagination. Six `DataSourceInformation` fields let Providers reject unsupported shapes before generating SQL; SELECT/EXPLAIN aggregation diagnostics are independent of data presence. The final Core 212/212 and Server/SDK 182/182 regressions include 36 new dedicated cases. Code was pushed in `5bbdc39041e724ced37e8e297f02a323311e7f40` before closure and state read-back. See the [contract and evidence](audits/measurement-join-196-closure-20260923.md); this does not claim a FreeSql release or compatibility negotiation with old servers.

The external issue list is intentionally separate from the existing nine-model gap catalog and from the internal PR sequence #382~#402. Closed scopes retain the issue URL, commit, test command and remaining boundary here and in the capability evidence index. #177/#180/#193 have completed functional delivery and verified issue closure; #184/#191 and new requests retain their documented acceptance work.
