# GitHub PR queue read-back (2026-09-25)

Repository: `https://github.com/IoTSharp/SonnetDB.git` (`origin`). `gh` is unavailable and default HTTPS requests failed, but `curl.exe --proxy http://127.0.0.1:7890` reached the GitHub REST API. The initial `GET /pulls?state=open&per_page=100` read-back returned exactly #164-#170; each PR detail confirmed `state=open`, `draft=false`, one commit and one changed file. The initial observations below predate the `main` push and later Dependabot closure on 2026-09-25.

A second live API read-back on 2026-09-25 again returned exactly open PR #164-#170 with the same seven head SHAs shown below. The open Issue API returned #184, #187, #189, #190, #191, #194, #195, #197 and #198. Local integration and tests have not changed their online states.

`git ls-remote origin 'refs/pull/*/merge'` returned merge refs only for #164-#170. Their merge commits use base `8a03ea01`, older than the inspected local `origin/main` (`91087889`). The PR API reports base SHA `d78b66fe` and `mergeable=null`, `mergeable_state=unknown` for each PR; GitHub mergeability is therefore not established. A fresh local `git merge-tree --write-tree origin/main origin/pr/<n>/head` returned exit 0 for each candidate:

| PR | Head | Head change from its merge base | Local merge simulation | GitHub head checks |
| --- | --- | --- | --- | --- |
| #164 | `f6f1fb96` | OpenTelemetry.Exporter.Console 1.18.0 -> 1.19.0 | Clean | 12 complete; [Ubuntu Build & Test failed](https://github.com/IoTSharp/SonnetDB/actions/runs/35409996271/job/105807543352) |
| #165 | `ad5103a7` | OpenTelemetry.Exporter.InMemory 1.18.0 -> 1.19.0 | Clean | 12 complete; [Ubuntu Build & Test failed](https://github.com/IoTSharp/SonnetDB/actions/runs/35410117928/job/105807903448) |
| #166 | `17be65a6` | OpenTelemetry.Exporter.OpenTelemetryProtocol 1.18.0 -> 1.19.0 | Clean | 12 complete; no failed checks |
| #167 | `175a821c` | OpenTelemetry.Extensions.Hosting 1.18.0 -> 1.19.0 | Clean | 12 complete; [Windows Build & Test failed](https://github.com/IoTSharp/SonnetDB/actions/runs/35410065043/job/105807746729) |
| #168 | `618f5af9` | OpenTelemetry.Instrumentation.AspNetCore 1.18.0 -> 1.19.0 | Clean | 12 complete; [Ubuntu](https://github.com/IoTSharp/SonnetDB/actions/runs/35410082998/job/105807800519) and [Windows](https://github.com/IoTSharp/SonnetDB/actions/runs/35410082998/job/105807800325) Build & Test failed |
| #169 | `a9c4584b` | OpenTelemetry.Instrumentation.Http 1.18.0 -> 1.19.0 | Clean | 12 complete; [Windows Build & Test failed](https://github.com/IoTSharp/SonnetDB/actions/runs/35410100741/job/105807854029) |
| #170 | `980f89b3` | Testcontainers 4.14.0 -> 4.15.0 | Clean | 12 complete; [Windows Build & Test failed](https://github.com/IoTSharp/SonnetDB/actions/runs/35410182905/job/105808090969) |

All seven heads change only `Directory.Packages.props` relative to their own merge base. Directly comparing an old head tree to current `main` also shows unrelated mainline changes. The seven package-version lines were applied locally, each matching the corresponding PR's own commit diff. The pinned `OpenTelemetry.Exporter.Prometheus.AspNetCore` 1.18.0-beta.1 is outside these PRs and remains unchanged. These package edits do not overlap the active GH-Issue #187/#198/#195 implementation files.

GitHub's public `GET /commits/<head>/check-runs` returned 12 completed checks per PR. `GET /actions/jobs/<id>` identifies the failed step as `Test` in every failed Build & Test job. Check-run output contains no failure summary; annotations only say `Process completed with exit code 1.`. Anonymous `GET /actions/jobs/105808090969/logs` returned HTTP 403, and the job page says `Sign in to view logs`. The workflow uploads TRX files, and the PR #170 run lists both test-result artifacts, but anonymous artifact download returned HTTP 401. The failing test name/error cannot be read here. PR base `d78b66fe` had no failed Build & Test checks, while the later main commit `91087889` has both Ubuntu and Windows Build & Test failures. These facts do not establish whether a dependency change, existing CI failure, or nondeterministic test caused any PR failure. Fresh local validation and current-base CI are required before treating the PRs as passing.

For #170, `dotnet restore src/Testcontainers.SonnetDB/Testcontainers.SonnetDB.csproj --force-evaluate` passed. `dotnet build src/Testcontainers.SonnetDB/Testcontainers.SonnetDB.csproj --no-restore` passed all five target frameworks with zero warnings and errors. `dotnet test tests/SonnetDB.Accuracy.Tests/SonnetDB.Accuracy.Tests.csproj --filter 'FullyQualifiedName~DockerBuildContextTests' -m:1` passed 1/1 with full project references, exercising the current Testcontainers Dockerfile archive implementation without Docker. Docker-backed integration and refreshed post-change GitHub checks remain pending.

For #164-#169, `git show origin/pr/<n>/head -- Directory.Packages.props` confirmed each exact 1.18.0 -> 1.19.0 change, and `git diff --check -- Directory.Packages.props` passed after local application. `dotnet restore tests/SonnetDB.Tests/SonnetDB.Tests.csproj --force-evaluate -m:1` passed. The Server and test project `project.assets.json` files resolve all six upgraded packages at 1.19.0. `dotnet build src/SonnetDB/SonnetDB.csproj --no-restore -m:1` passed with zero warnings/errors. `dotnet test tests/SonnetDB.Tests/SonnetDB.Tests.csproj --no-restore -m:1 --filter 'FullyQualifiedName~ObservabilityBootstrapTests|FullyQualifiedName~ObservabilityTraceEndToEndTests'` passed 3/3, and the separate `PrometheusEndpointTests` run with `--no-build --no-restore` passed 1/1. This is a focused local regression result, not a replacement for the historical failed PR checks or a full cross-platform CI rerun.

## Post-push closure read-back

Commit `1250a617` put the seven exact PR-head version changes on `main`; the complete integration was pushed through `d49fc87c43adf542b2fae3f93dd41a21650dde63`. A later live `GET /pulls?state=open&per_page=100` returned an empty array. Individual PR and issue-event API reads show #164-#170 all `state=closed`, `merged=false`, closed by `dependabot[bot]` between 00:30 and 00:32 UTC on 2026-09-25. Their head commits were not merged through GitHub PRs; the equivalent package lines were already in `main`, so reopening to create empty merge commits would add no implementation.

The #164 event timeline has one caveat: Dependabot closed its 1.18.0 -> 1.19.0 head, then renamed the closed PR title from Console 1.19.0 to 1.19.1. The PR head `f6f1fb96` and current `main` both contain Console 1.19.0. Do not treat the later 1.19.1 title as implemented or verified. Historical failed head checks remain failed; the post-push `main` CI outcome is a separate gate.
