# M39 #339 Document / measurement 准入证据

日期：2026-09-08。状态：**Document / measurement 原生事件与批次幂等准入 PASS；本切片不实现 SQL 触发器运行时**。

复现命令（Windows x64、PowerShell 7、.NET 10）：

```text
dotnet run --project tests/SonnetDB.TriggerAdmission/SonnetDB.TriggerAdmission.csproj --no-build -- --quick --output artifacts/m39-339-admission
dotnet run --project tests/SonnetDB.TriggerAdmission/SonnetDB.TriggerAdmission.csproj --no-build -- --output artifacts/m39-339-admission-full
dotnet run --project tests/SonnetDB.TriggerAdmission/SonnetDB.TriggerAdmission.csproj --no-build -- --output artifacts/m39-339-admission-full-closed
```

本次 quick 运行在 DEVPER（22 logical processors，Windows 10.0.26200，.NET 10.0.11）完成 8 个有界样本；closed full 运行完成 14 个样本，覆盖 1、100、10,000 点及 1,000 series。两次临时目录均在所有数据库关闭后完成安全校验和删除。机器可读报告已归档为 [`m39-339-admission-full-closed.json`](evidence/m39-339-admission-20260908/m39-339-admission-full-closed.json)，SHA-256 为 `c0d9ed03bc091bc951d7e2dbc0622a84b22107153409cb13ea66b28ba25fd313`，Document 与 measurement 决策均为 `PASS`。

## Document

使用公开 `DocumentCollectionManager` / `DocumentCollectionStore` API，覆盖 24 条文档、24 个分类基数的索引写入查询、更新/删除、change feed 和重开恢复。实测写入查询 278.693 ms、9379 WAL bytes、393384 managed bytes；更新/删除 55.380 ms、3412 WAL bytes；feed 有 32 条记录；重开保留 22 条文档。

Document 已通过准入：change feed 持久化 `cause`、`requestId`、批内 `operationIndex` 和受限 `changedPaths` patch 描述；旧记录缺失字段按 user/null 兼容读取；TTL 清理标记为 `ttl`，bulk 事件标记为 `bulk`，幂等 replay 直接返回持久结果而不产生新业务事件。闭环测试覆盖 bulk 元数据、重开和索引恢复；固定七天保留窗口仍由维护策略统一管理。

## measurement

使用 `Tsdb.WriteMany` 与原生查询验证有序、逆序、重复重放和高基数：

| 场景 | 输入 | 结果 | elapsed | managed bytes | WAL bytes | data bytes |
|---|---:|---:|---:|---:|---:|---:|
| ordered write | 1 / 100 / 10,000 points | 原样恢复 | 201.330 / 46.770 / 119.880 ms | runner report | 264 each | runner report |
| out-of-order | 1 / 100 / 10,000 points | 按时间查询有序 | 48.850 / 44.960 / 105.890 ms | runner report | 264 each | runner report |
| duplicate replay | 1 / 100 / 10,000 × 2 | durable batch replay 保持 1 / 100 / 10,000 点，重复点为 0 | 93.230 / 59.520 / 203.990 ms | runner report | 264 each | runner report |
| high cardinality | 1,000 series | 1,000 catalog entries，抽样 64 | 124.700 ms | runner report | 264 | runner report |

measurement 已通过准入：新增持久 batch ledger（pending/committed、SHA-256 payload、旧格式兼容），`WriteMany` 和 `BulkIngestor` 接受稳定 batch ID；相同 payload 的重放返回 0，冲突 payload 拒绝，重开后仍有效。pending 重试按字段和时间点精确对拍补缺，避免 checkpoint/segment 窗口重复；普通无 batch ID 的乱序和同 timestamp 写入仍保留原生多重集合。

## 决定与后续门禁

本证据实现了原生事件 envelope、批次身份、接受顺序、cause/patch 描述和重开幂等恢复；没有把关系表 `OLD` / `NEW` 套到 document patch 或 measurement batch。固定硬件和长期容量门禁仍独立执行。
